# Vue.js/Nuxt.js テスト設計思想ガイド

## はじめに

このガイドでは、Vue.js/Nuxt.js アプリケーションにおけるテスト設計の思想と実践的なアプローチを解説します。テストは単なる品質保証のツールではなく、設計を改善し、開発速度を向上させる重要な開発プラクティスです。

### テストピラミッドとは

```
        /\
       /  \  E2E Tests (少数)
      /____\
     /      \
    / Integration \ Integration Tests (中程度)
   /____________\
  /              \
 /  Unit Tests    \ Unit Tests (多数)
/__________________\
```

**なぜピラミッド型なのか？**

- **Unit Tests**: 高速、安定、デバッグしやすい
- **Integration Tests**: 実際の連携を検証、適度な実行時間
- **E2E Tests**: ユーザー体験を検証、実行が重く不安定になりがち

## Vue Test Utils 実戦ガイド

### DOM 要素の存在・表示確認

Vue Test Utils で最も頻繁に使用される検証パターンです。

```typescript
// DOM要素の存在確認
test("エラーメッセージが表示される", () => {
  const wrapper = mount(LoginForm, {
    props: { hasError: true },
  });

  expect(wrapper.find(".error-message").exists()).toBe(true);
  expect(wrapper.find('[data-testid="error-alert"]').exists()).toBe(true);
});

// 要素の表示状態確認
test("ローディング中は送信ボタンが非表示", () => {
  const wrapper = mount(ContactForm, {
    props: { isLoading: true },
  });

  expect(wrapper.find(".loading-spinner").isVisible()).toBe(true);
  expect(wrapper.find(".submit-button").isVisible()).toBe(false);
});

// コンポーネントの存在確認
test("管理者権限時に管理パネルが表示される", () => {
  const wrapper = mount(Dashboard, {
    props: { userRole: "admin" },
  });

  expect(wrapper.findComponent(AdminPanel).exists()).toBe(true);
  expect(wrapper.findComponent(UserPanel).exists()).toBe(false);
});
```

### テキスト内容の検証

```typescript
// 完全一致でのテキスト確認
test("ユーザー名が正しく表示される", () => {
  const wrapper = mount(UserProfile, {
    props: { user: { name: "John Doe" } },
  });

  expect(wrapper.find("h1").text()).toBe("John Doe");
  expect(wrapper.find(".username").text()).toBe("John Doe");
});

// 部分一致でのテキスト確認
test("メッセージに重要な情報が含まれている", () => {
  const wrapper = mount(NotificationBar);

  expect(wrapper.find(".notification").text()).toContain("重要");
  expect(wrapper.text()).toContain("システムメンテナンス");
});

// HTML構造の確認
test("マークアップが正しく生成される", () => {
  const wrapper = mount(RichTextEditor, {
    props: { content: "<strong>太字</strong>" },
  });

  expect(wrapper.html()).toContain("<strong>太字</strong>");
  expect(wrapper.find(".editor-content").html()).toContain('class="highlight"');
});
```

### 属性・クラスの検証

```typescript
// 属性値の確認
test("フォームフィールドの属性が正しく設定される", () => {
  const wrapper = mount(EmailInput, {
    props: { required: true, disabled: false },
  });

  expect(wrapper.find("input").attributes("type")).toBe("email");
  expect(wrapper.find("input").attributes("required")).toBeDefined();
  expect(wrapper.find("input").attributes("disabled")).toBeUndefined();
  expect(wrapper.find("input").attributes("placeholder")).toBe(
    "メールアドレス"
  );
});

// CSSクラスの確認
test("状態に応じてクラスが適用される", () => {
  const wrapper = mount(StatusButton, {
    props: { status: "active", size: "large" },
  });

  expect(wrapper.find(".button").classes()).toContain("active");
  expect(wrapper.find(".button").classes()).toContain("btn-large");
  expect(wrapper.find(".button").classes("btn-primary")).toBe(true);
  expect(wrapper.find(".button").classes("disabled")).toBe(false);
});

// 動的クラスの確認
test("条件に応じてクラスが動的に変更される", async () => {
  const wrapper = mount(ToggleButton);

  expect(wrapper.find(".toggle").classes("active")).toBe(false);

  await wrapper.find(".toggle").trigger("click");

  expect(wrapper.find(".toggle").classes("active")).toBe(true);
});
```

### フォーム要素の検証

```typescript
// input要素の値確認
test("入力値が正しく反映される", async () => {
  const wrapper = mount(SearchForm);
  const input = wrapper.find('input[type="text"]');

  await input.setValue("検索キーワード");

  expect(input.element.value).toBe("検索キーワード");
  expect(wrapper.vm.searchQuery).toBe("検索キーワード");
});

// チェックボックスの状態確認
test("チェックボックスの状態管理", async () => {
  const wrapper = mount(SettingsForm);
  const checkbox = wrapper.find('input[type="checkbox"]');

  expect(checkbox.element.checked).toBe(false);

  await checkbox.setChecked(true);

  expect(checkbox.element.checked).toBe(true);
  expect(wrapper.emitted().change).toBeTruthy();
});

// セレクトボックスの値確認
test("セレクトボックスの選択値", async () => {
  const wrapper = mount(CategorySelector);
  const select = wrapper.find("select");

  await select.setValue("category2");

  expect(select.element.value).toBe("category2");
  expect(wrapper.vm.selectedCategory).toBe("category2");
});

// ラジオボタンの確認
test("ラジオボタンの選択", async () => {
  const wrapper = mount(PaymentForm);
  const radio = wrapper.find('input[value="credit"]');

  await radio.setChecked();

  expect(radio.element.checked).toBe(true);
  expect(wrapper.vm.paymentMethod).toBe("credit");
});
```

### イベントの検証

```typescript
// イベント発火の確認
test("ボタンクリック時にイベントが発火される", async () => {
  const wrapper = mount(ActionButton);

  await wrapper.find("button").trigger("click");

  expect(wrapper.emitted().click).toBeTruthy();
  expect(wrapper.emitted("action-performed")).toBeTruthy();
});

// イベントの発火回数確認
test("ダブルクリック防止機能", async () => {
  const wrapper = mount(SubmitButton);
  const button = wrapper.find("button");

  await button.trigger("click");
  await button.trigger("click");

  expect(wrapper.emitted().submit).toHaveLength(1); // 1回のみ発火
});

// イベントの引数確認
test("カスタムイベントのペイロード確認", async () => {
  const wrapper = mount(UserCard, {
    props: { user: { id: 123, name: "John" } },
  });

  await wrapper.find(".edit-button").trigger("click");

  expect(wrapper.emitted().edit[0]).toEqual([123]); // user.idが引数として渡される
  expect(wrapper.emitted("user-selected")[0]).toEqual([
    { id: 123, name: "John" },
  ]);
});

// 複合イベントの確認
test("フォーム送信時の複数イベント", async () => {
  const wrapper = mount(ContactForm);

  await wrapper.find("form").trigger("submit");

  expect(wrapper.emitted().submit).toBeTruthy();
  expect(wrapper.emitted("form-validated")).toBeTruthy();
  expect(wrapper.emitted("tracking-event")).toBeTruthy();
});
```

### Props とデータの検証

```typescript
// プロパティの確認
test("propsが正しく渡される", () => {
  const user = { id: 1, name: "Alice" };
  const wrapper = mount(UserProfile, {
    props: { user, isEditable: true },
  });

  expect(wrapper.props("user")).toEqual(user);
  expect(wrapper.props("isEditable")).toBe(true);
});

// リアクティブデータの確認
test("コンポーネントの内部状態", async () => {
  const wrapper = mount(Counter);

  expect(wrapper.vm.count).toBe(0);

  await wrapper.find(".increment").trigger("click");

  expect(wrapper.vm.count).toBe(1);
});

// 算出プロパティの確認
test("算出プロパティが正しく計算される", () => {
  const wrapper = mount(UserStats, {
    props: {
      users: [
        { id: 1, isActive: true },
        { id: 2, isActive: false },
        { id: 3, isActive: true },
      ],
    },
  });

  expect(wrapper.vm.activeUserCount).toBe(2);
  expect(wrapper.vm.inactiveUserCount).toBe(1);
});

// ウォッチャーの動作確認
test("ウォッチャーが適切に動作する", async () => {
  const wrapper = mount(SearchComponent);

  wrapper.vm.searchTerm = "new search";
  await wrapper.vm.$nextTick();

  expect(wrapper.vm.searchResults).toHaveLength(0); // デバウンス処理のため初期は空

  // デバウンス時間経過後
  await new Promise((resolve) => setTimeout(resolve, 300));

  expect(wrapper.vm.searchResults.length).toBeGreaterThan(0);
});
```

### 子コンポーネントの検証

```typescript
// 子コンポーネントの存在と props確認
test("子コンポーネントに正しいpropsが渡される", () => {
  const wrapper = mount(UserList, {
    props: { users: mockUsers },
  });

  const userCards = wrapper.findAllComponents(UserCard);
  expect(userCards).toHaveLength(mockUsers.length);

  expect(userCards[0].props("user")).toEqual(mockUsers[0]);
  expect(userCards[0].props("isEditable")).toBe(true);
});

// 子コンポーネントのイベント処理
test("子コンポーネントからのイベントハンドリング", async () => {
  const wrapper = mount(TodoList);
  const todoItem = wrapper.findComponent(TodoItem);

  // 子コンポーネントのイベントをトリガー
  await todoItem.vm.$emit("remove", 123);

  expect(wrapper.emitted("todo-removed")).toBeTruthy();
  expect(wrapper.emitted("todo-removed")[0]).toEqual([123]);
});

// 動的コンポーネントの確認
test("条件に応じて異なるコンポーネントがレンダリングされる", () => {
  const wrapper = mount(DynamicView, {
    props: { viewType: "admin" },
  });

  expect(wrapper.findComponent(AdminView).exists()).toBe(true);
  expect(wrapper.findComponent(UserView).exists()).toBe(false);
});

// ネストした子コンポーネント
test("深くネストしたコンポーネントの検証", () => {
  const wrapper = mount(Dashboard);

  const sidebar = wrapper.findComponent(Sidebar);
  const navigation = sidebar.findComponent(Navigation);
  const menuItem = navigation.findComponent(MenuItem);

  expect(menuItem.props("isActive")).toBe(true);
});
```

### 条件付きレンダリングの検証

```typescript
// v-if による条件付きレンダリング
test("v-if による要素の表示/非表示", async () => {
  const wrapper = mount(ConditionalComponent, {
    props: { showContent: false },
  });

  // v-ifの場合、DOM要素自体が存在しない
  expect(wrapper.find(".conditional-content").exists()).toBe(false);

  await wrapper.setProps({ showContent: true });

  expect(wrapper.find(".conditional-content").exists()).toBe(true);
});

// v-show による表示制御
test("v-show による要素の表示制御", async () => {
  const wrapper = mount(ToggleComponent, {
    data() {
      return { isVisible: false };
    },
  });

  // v-showの場合、DOM要素は存在するが非表示
  expect(wrapper.find(".toggle-content").exists()).toBe(true);
  expect(wrapper.find(".toggle-content").isVisible()).toBe(false);
  expect(wrapper.find(".toggle-content").attributes("style")).toContain(
    "display: none"
  );

  wrapper.vm.isVisible = true;
  await wrapper.vm.$nextTick();

  expect(wrapper.find(".toggle-content").isVisible()).toBe(true);
});

// 複雑な条件分岐
test("複数条件での表示制御", () => {
  const wrapper = mount(StatusComponent, {
    props: {
      isLoading: false,
      hasError: false,
      hasData: true,
    },
  });

  expect(wrapper.find(".loading").exists()).toBe(false);
  expect(wrapper.find(".error").exists()).toBe(false);
  expect(wrapper.find(".content").exists()).toBe(true);
});
```

### リストレンダリングの検証

```typescript
// v-for による一覧表示
test("v-for でリストが正しくレンダリングされる", () => {
  const items = [
    { id: 1, name: "アイテム1" },
    { id: 2, name: "アイテム2" },
    { id: 3, name: "アイテム3" },
  ];

  const wrapper = mount(ItemList, {
    props: { items },
  });

  const listItems = wrapper.findAll(".list-item");
  expect(listItems).toHaveLength(3);

  expect(listItems[0].text()).toBe("アイテム1");
  expect(listItems[1].text()).toBe("アイテム2");
  expect(listItems[2].text()).toBe("アイテム3");
});

// 動的リストの操作
test("リストアイテムの動的な追加・削除", async () => {
  const wrapper = mount(DynamicList);

  expect(wrapper.findAll(".item")).toHaveLength(0);

  await wrapper.find(".add-button").trigger("click");

  expect(wrapper.findAll(".item")).toHaveLength(1);

  await wrapper.find(".remove-button").trigger("click");

  expect(wrapper.findAll(".item")).toHaveLength(0);
});

// リストのフィルタリング
test("検索によるリストフィルタリング", async () => {
  const wrapper = mount(FilterableList, {
    props: {
      items: ["apple", "banana", "cherry", "apricot"],
    },
  });

  expect(wrapper.findAll(".item")).toHaveLength(4);

  await wrapper.find(".search-input").setValue("ap");

  expect(wrapper.findAll(".item")).toHaveLength(2); // apple, apricot
});
```

### スロット内容の検証

```typescript
// デフォルトスロット
test("スロット内容が正しく表示される", () => {
  const wrapper = mount(Card, {
    slots: {
      default: "<p>カード内容</p>",
    },
  });

  expect(wrapper.find(".card-content").html()).toContain("<p>カード内容</p>");
  expect(wrapper.text()).toContain("カード内容");
});

// 名前付きスロット
test("名前付きスロットが正しく配置される", () => {
  const wrapper = mount(Layout, {
    slots: {
      header: "<h1>ヘッダー</h1>",
      content: "<p>メイン内容</p>",
      footer: "<footer>フッター</footer>",
    },
  });

  expect(wrapper.find('[data-testid="header-slot"]').html()).toContain(
    "<h1>ヘッダー</h1>"
  );
  expect(wrapper.find('[data-testid="content-slot"]').html()).toContain(
    "<p>メイン内容</p>"
  );
  expect(wrapper.find('[data-testid="footer-slot"]').html()).toContain(
    "<footer>フッター</footer>"
  );
});

// スコープ付きスロット
test("スコープ付きスロットのデータ受け渡し", () => {
  const wrapper = mount(DataProvider, {
    props: { items: ["item1", "item2"] },
    slots: {
      default: `
        <template #default="{ items, currentIndex }">
          <div class="slot-content">{{ items[currentIndex] }}</div>
        </template>
      `,
    },
  });

  expect(wrapper.find(".slot-content").text()).toBe("item1");
});
```

### 実戦でよく使うパターン

```typescript
// ユーザーインタラクション後の総合検証
test("ログインフォーム送信の一連の流れ", async () => {
  const wrapper = mount(LoginForm);

  // 初期状態の確認
  expect(wrapper.find(".submit-button").attributes("disabled")).toBeDefined();
  expect(wrapper.find(".error-message").exists()).toBe(false);

  // フォーム入力
  await wrapper.find('input[type="email"]').setValue("user@example.com");
  await wrapper.find('input[type="password"]').setValue("password123");

  // 入力後の状態確認
  expect(wrapper.find(".submit-button").attributes("disabled")).toBeUndefined();

  // フォーム送信
  await wrapper.find("form").trigger("submit");

  // 送信後の状態確認
  expect(wrapper.emitted().submit).toBeTruthy();
  expect(wrapper.find(".loading-spinner").isVisible()).toBe(true);
  expect(wrapper.find(".submit-button").text()).toBe("送信中...");
});

// 非同期処理の完全な検証
test("APIコール後のUI更新", async () => {
  const mockResponse = { id: 1, name: "John" };
  vi.mocked($fetch).mockResolvedValue(mockResponse);

  const wrapper = mount(UserProfile, {
    props: { userId: 1 },
  });

  // ローディング状態の確認
  expect(wrapper.find(".loading").isVisible()).toBe(true);
  expect(wrapper.find(".user-content").exists()).toBe(false);

  // 非同期処理完了まで待機
  await flushPromises();

  // データ取得後の状態確認
  expect(wrapper.find(".loading").exists()).toBe(false);
  expect(wrapper.find(".user-content").isVisible()).toBe(true);
  expect(wrapper.find(".user-name").text()).toBe("John");
});

// 複数のコンポーネント間での連携
test("親子コンポーネント間のデータ連携", async () => {
  const wrapper = mount(TodoApp);

  // 新しいTodoの追加
  await wrapper.find(".add-input").setValue("新しいタスク");
  await wrapper.find(".add-button").trigger("click");

  // リストに追加されることを確認
  const todoItems = wrapper.findAllComponents(TodoItem);
  expect(todoItems).toHaveLength(1);
  expect(todoItems[0].props("todo")).toMatchObject({
    text: "新しいタスク",
    completed: false,
  });

  // 完了状態の切り替え
  await todoItems[0].find(".toggle-checkbox").setChecked(true);

  // 親コンポーネントの状態が更新されることを確認
  expect(wrapper.vm.completedCount).toBe(1);
  expect(wrapper.find(".completed-count").text()).toBe("完了: 1");
});
```

### テストのベストプラクティス

```typescript
// 1. data-testid属性を使用したセレクタ
test("意味のあるセレクタを使用", () => {
  const wrapper = mount(UserCard);

  // ❌ CSSクラスに依存（変更されやすい）
  expect(wrapper.find(".btn-primary").exists()).toBe(true);

  // ✅ data-testidを使用（テスト専用の識別子）
  expect(wrapper.find('[data-testid="submit-button"]').exists()).toBe(true);
  expect(wrapper.find('[data-testid="user-avatar"]').exists()).toBe(true);
});

// 2. 適切な抽象化レベル
test("ユーザーの視点でテスト", async () => {
  const wrapper = mount(ShoppingCart);

  // ❌ 内部実装に依存
  expect(wrapper.vm.internalCartState.items).toHaveLength(0);

  // ✅ ユーザーが見える部分をテスト
  expect(wrapper.find('[data-testid="cart-count"]').text()).toBe("0");

  await wrapper.find('[data-testid="add-to-cart"]').trigger("click");

  expect(wrapper.find('[data-testid="cart-count"]').text()).toBe("1");
});

// 3. 明確で保守しやすいテスト
test("テストケースの意図を明確にする", async () => {
  // テストケースごとに独立したセットアップ
  const user = createUser({ role: "admin" });
  const wrapper = mount(AdminPanel, {
    props: { currentUser: user },
  });

  // 何をテストしているかが明確
  expect(wrapper.find('[data-testid="admin-controls"]').isVisible()).toBe(true);
  expect(wrapper.find('[data-testid="user-management"]').exists()).toBe(true);

  // 期待される結果が明確
  await wrapper.find('[data-testid="delete-user-btn"]').trigger("click");
  expect(wrapper.emitted("user-deletion-requested")).toBeTruthy();
});

// 4. 境界値とエラーケースのテスト
test("エラーケースの適切なハンドリング", async () => {
  vi.mocked($fetch).mockRejectedValue(new Error("Network Error"));

  const wrapper = mount(DataFetcher);

  await flushPromises();

  expect(wrapper.find('[data-testid="error-message"]').isVisible()).toBe(true);
  expect(wrapper.find('[data-testid="retry-button"]').exists()).toBe(true);
  expect(wrapper.find('[data-testid="content"]').exists()).toBe(false);
});
```

## コンポーネントテスト

### 基本方針

コンポーネントテストでは「**ブラックボックステスト**」の考え方を採用します。つまり、内部実装ではなく外部仕様（入力と出力）に注目します。

```typescript
// ❌ 悪い例: 内部実装をテスト
test("内部のdataプロパティが変更される", () => {
  const wrapper = mount(Counter);
  wrapper.vm.internalCount = 5; // 内部実装に依存
  expect(wrapper.vm.internalCount).toBe(5);
});

// ✅ 良い例: 振る舞いをテスト
test("ボタンクリック時にカウンターが増加する", async () => {
  const wrapper = mount(Counter);
  await wrapper.find('[data-testid="increment-btn"]').trigger("click");
  expect(wrapper.text()).toContain("1");
});
```

### Props・Emits 関連

**なぜ Props・Emits のテストが重要か？**

コンポーネントの「契約」を定義する部分だからです。この契約が守られることで、親子コンポーネント間の連携が保証されます。

```typescript
// props検証の実例
describe("UserCard component", () => {
  test("必須propsが未指定時にwarningが出力される", () => {
    const spy = vi.spyOn(console, "warn").mockImplementation(() => {});
    mount(UserCard); // nameプロパティが必須だが未指定
    expect(spy).toHaveBeenCalledWith(
      expect.stringContaining("Missing required prop")
    );
    spy.mockRestore();
  });

  test("emitが適切なペイロードで発火する", async () => {
    const wrapper = mount(UserCard, {
      props: { user: mockUser },
    });

    await wrapper.find('[data-testid="edit-btn"]').trigger("click");

    expect(wrapper.emitted("edit")).toBeTruthy();
    expect(wrapper.emitted("edit")[0]).toEqual([mockUser.id]);
  });
});
```

**ベストプラクティス:**

- テストデータはファクトリーパターンで作成
- 境界値（空文字、null、undefined）を含めてテスト
- TypeScript の型チェックに頼らず、実行時の検証も実装

### レンダリングテスト

**条件分岐テストの考え方**

```typescript
// 条件分岐の網羅的テスト
describe("ConditionalComponent", () => {
  test.each([
    ["loading状態", { isLoading: true, hasError: false }, "ローディング中..."],
    [
      "エラー状態",
      { isLoading: false, hasError: true },
      "エラーが発生しました",
    ],
    ["成功状態", { isLoading: false, hasError: false }, "データを表示"],
  ])("%s のとき適切に表示される", async (_, props, expectedText) => {
    const wrapper = mount(ConditionalComponent, { props });
    expect(wrapper.text()).toContain(expectedText);
  });
});
```

### ユーザーインタラクション

**非同期処理のテスト**

```typescript
// 非同期処理を含むインタラクション
test("フォーム送信時にローディング状態が適切に管理される", async () => {
  const wrapper = mount(ContactForm);
  const submitBtn = wrapper.find('[data-testid="submit-btn"]');

  // 送信前の状態確認
  expect(submitBtn.text()).toBe("送信");
  expect(submitBtn.attributes("disabled")).toBeUndefined();

  // 送信トリガー
  await submitBtn.trigger("click");

  // ローディング中の状態確認
  expect(submitBtn.text()).toBe("送信中...");
  expect(submitBtn.attributes("disabled")).toBeDefined();

  // 非同期処理完了まで待機
  await wrapper.vm.$nextTick();
  await new Promise((resolve) => setTimeout(resolve, 0));

  // 送信完了後の状態確認
  expect(submitBtn.text()).toBe("送信");
  expect(submitBtn.attributes("disabled")).toBeUndefined();
});
```

## Composables テスト

### テスト対象の選び方

Composables は**ピュアなロジック**を含むため、単体テストに最適です。

```typescript
// useCounter.test.ts
import { useCounter } from "~/composables/useCounter";

describe("useCounter", () => {
  test("初期値が正しく設定される", () => {
    const { count } = useCounter(5);
    expect(count.value).toBe(5);
  });

  test("increment関数でカウントが増加する", () => {
    const { count, increment } = useCounter(0);
    increment();
    expect(count.value).toBe(1);
  });

  test("computed値が状態変更に追従する", () => {
    const { count, increment, isEven } = useCounter(0);
    expect(isEven.value).toBe(true);

    increment();
    expect(isEven.value).toBe(false);
  });
});
```

### API 通信のテスト戦略

**モックの使い分け**

```typescript
// $fetchをモックする例
describe("useApi", () => {
  beforeEach(() => {
    // 各テストで独立したモックを作成
    vi.clearAllMocks();
  });

  test("成功時のレスポンス処理", async () => {
    const mockData = { id: 1, name: "Test User" };
    const mockFetch = vi.fn().mockResolvedValue(mockData);

    // $fetchをモック
    vi.stubGlobal("$fetch", mockFetch);

    const { fetchUser, data, isLoading } = useApi();

    expect(isLoading.value).toBe(false);

    await fetchUser(1);

    expect(mockFetch).toHaveBeenCalledWith("/api/users/1");
    expect(data.value).toEqual(mockData);
    expect(isLoading.value).toBe(false);
  });

  test("エラー時の処理", async () => {
    const mockError = new Error("Network Error");
    const mockFetch = vi.fn().mockRejectedValue(mockError);

    vi.stubGlobal("$fetch", mockFetch);

    const { fetchUser, error, isLoading } = useApi();

    await fetchUser(1);

    expect(error.value).toBe(mockError);
    expect(isLoading.value).toBe(false);
  });
});
```

## Store/Pinia テスト

### 状態管理テストの原則

1. **アクションのテスト**: 副作用と状態変更を分離して検証
2. **ゲッターのテスト**: 純粋関数として扱う
3. **初期状態のテスト**: 予期しない初期値を防ぐ

```typescript
// userStore.test.ts
describe("userStore", () => {
  let store: ReturnType<typeof useUserStore>;

  beforeEach(() => {
    setActivePinia(createPinia());
    store = useUserStore();
  });

  test("初期状態が正しく設定される", () => {
    expect(store.users).toEqual([]);
    expect(store.isLoading).toBe(false);
    expect(store.error).toBeNull();
  });

  test("fetchUsers アクションが状態を正しく更新する", async () => {
    const mockUsers = [{ id: 1, name: "User 1" }];
    vi.mocked($fetch).mockResolvedValue(mockUsers);

    await store.fetchUsers();

    expect(store.users).toEqual(mockUsers);
    expect(store.isLoading).toBe(false);
  });

  test("activeUsers ゲッターが正しく計算される", () => {
    store.$patch({
      users: [
        { id: 1, name: "Active User", isActive: true },
        { id: 2, name: "Inactive User", isActive: false },
      ],
    });

    expect(store.activeUsers).toHaveLength(1);
    expect(store.activeUsers[0].name).toBe("Active User");
  });
});
```

## ページ・レイアウトテスト

### メタデータテストの重要性

SEO やソーシャルメディア共有において、正しいメタデータは極めて重要です。

```typescript
// pages/users/[id].test.ts
describe("User Detail Page", () => {
  test("動的メタデータが正しく生成される", async () => {
    const mockUser = { id: 1, name: "John Doe", bio: "Software Developer" };

    const nuxtApp = await renderSuspended(UserDetailPage, {
      route: "/users/1",
      data: {
        user: mockUser,
      },
    });

    expect(nuxtApp.ssrContext?.head.title).toBe("John Doe - User Profile");
    expect(nuxtApp.ssrContext?.head.meta).toContainEqual({
      property: "og:title",
      content: "John Doe - User Profile",
    });
  });
});
```

## ユーティリティ関数テスト

### 純粋関数テストのベストプラクティス

```typescript
// utils/formatters.test.ts
describe("formatDate", () => {
  test.each([
    ["2024-01-15", "ja", "2024年1月15日"],
    ["2024-01-15", "en", "January 15, 2024"],
    ["invalid-date", "ja", ""],
    [null, "ja", ""],
    [undefined, "ja", ""],
  ])("formatDate(%s, %s) は %s を返す", (input, locale, expected) => {
    expect(formatDate(input, locale)).toBe(expected);
  });

  test("タイムゾーンを考慮した日付フォーマット", () => {
    const date = "2024-01-15T00:00:00Z";
    const formatted = formatDate(date, "ja", { timeZone: "Asia/Tokyo" });
    expect(formatted).toBe("2024年1月15日");
  });
});
```

## 統合テスト要素

### プラグイン連携テスト

```typescript
// プラグインとの統合テスト例
describe("i18n Plugin Integration", () => {
  test("言語切り替えが正しく動作する", async () => {
    const wrapper = mount(LanguageSwitcher, {
      global: {
        plugins: [i18n],
      },
    });

    const switchBtn = wrapper.find('[data-testid="lang-switch"]');
    await switchBtn.trigger("click");

    expect(wrapper.find('[data-testid="greeting"]').text()).toBe("Hello");
  });
});
```

## 高度なテスト手法

### テスト駆動開発（TDD）サイクル

```
Red → Green → Refactor
🔴    🟢     🔵
```

**実践例:**

```typescript
// 1. Red: まずテストを書く（失敗する）
test("税込価格を計算できる", () => {
  expect(calculateTaxIncluded(100, 0.1)).toBe(110);
});

// 2. Green: テストが通る最低限の実装
function calculateTaxIncluded(price: number, taxRate: number): number {
  return price + price * taxRate;
}

// 3. Refactor: コードを改善
function calculateTaxIncluded(price: number, taxRate: number): number {
  if (price < 0 || taxRate < 0) {
    throw new Error("価格と税率は負の値にできません");
  }
  return Math.round(price * (1 + taxRate));
}
```

### 境界値テスト

ソフトウェアのバグは境界条件で発生しやすいため、境界値を重点的にテストします。

```typescript
describe("ページネーション", () => {
  test.each([
    [1, true], // 最初のページ
    [10, false], // 中間のページ
    [100, true], // 最後のページ
    [0, false], // 境界外（下限）
    [101, false], // 境界外（上限）
  ])("ページ %d の無効状態は %s", (page, isDisabled) => {
    const result = isPageDisabled(page, 100);
    expect(result).toBe(isDisabled);
  });
});
```

### Property-Based Testing

```typescript
import { fc } from "fast-check";

test("ソート関数の不変条件", () => {
  fc.assert(
    fc.property(fc.array(fc.integer()), (arr) => {
      const sorted = customSort(arr);

      // 不変条件1: 長さが変わらない
      expect(sorted).toHaveLength(arr.length);

      // 不変条件2: 同じ要素を含む
      expect(sorted.slice().sort()).toEqual(arr.slice().sort());

      // 不変条件3: ソート済み
      for (let i = 1; i < sorted.length; i++) {
        expect(sorted[i]).toBeGreaterThanOrEqual(sorted[i - 1]);
      }
    })
  );
});
```

## テスト品質向上のポイント

### テストデータ管理

**ファクトリーパターンの活用**

```typescript
// testUtils/factories.ts
export const createUser = (overrides: Partial<User> = {}): User => ({
  id: 1,
  name: "Test User",
  email: "test@example.com",
  isActive: true,
  createdAt: new Date("2024-01-01"),
  ...overrides,
});

export const createUsers = (
  count: number,
  overrides: Partial<User> = {}
): User[] =>
  Array.from({ length: count }, (_, i) =>
    createUser({ id: i + 1, ...overrides })
  );

// テストでの使用例
test("アクティブユーザーのフィルタリング", () => {
  const users = [
    createUser({ isActive: true }),
    createUser({ isActive: false }),
    createUser({ isActive: true }),
  ];

  const activeUsers = filterActiveUsers(users);
  expect(activeUsers).toHaveLength(2);
});
```

### モック戦略

**依存関係の注入とモック**

```typescript
// 依存関係を注入可能な設計
class ApiService {
  constructor(private httpClient: HttpClient) {}

  async getUser(id: number): Promise<User> {
    return this.httpClient.get(`/users/${id}`);
  }
}

// テストでモックを注入
test("APIサービスのテスト", async () => {
  const mockHttpClient = {
    get: vi.fn().mockResolvedValue(mockUser),
  };

  const apiService = new ApiService(mockHttpClient as any);
  const result = await apiService.getUser(1);

  expect(mockHttpClient.get).toHaveBeenCalledWith("/users/1");
  expect(result).toEqual(mockUser);
});
```

### 時間依存処理のテスト

```typescript
// 時間をモックする
test("1分後にタイムアウトする", async () => {
  vi.useFakeTimers();

  const onTimeout = vi.fn();
  const timer = new Timer(60000, onTimeout);
  timer.start();

  // 59秒経過
  vi.advanceTimersByTime(59000);
  expect(onTimeout).not.toHaveBeenCalled();

  // 1秒追加で合計60秒
  vi.advanceTimersByTime(1000);
  expect(onTimeout).toHaveBeenCalledOnce();

  vi.useRealTimers();
});
```

## アクセシビリティテスト

```typescript
import { axe, toHaveNoViolations } from "jest-axe";

expect.extend(toHaveNoViolations);

test("コンポーネントにアクセシビリティ違反がない", async () => {
  const wrapper = mount(UserForm);
  const results = await axe(wrapper.element);
  expect(results).toHaveNoViolations();
});

test("キーボードナビゲーションが動作する", async () => {
  const wrapper = mount(Modal);

  // Escapeキーでモーダルが閉じる
  await wrapper.trigger("keydown", { key: "Escape" });
  expect(wrapper.emitted("close")).toBeTruthy();

  // Tabキーでフォーカス移動
  const firstInput = wrapper.find('input[type="text"]');
  firstInput.element.focus();

  await wrapper.trigger("keydown", { key: "Tab" });

  const secondInput = wrapper.find('input[type="email"]');
  expect(document.activeElement).toBe(secondInput.element);
});
```

## パフォーマンステスト

```typescript
test("大量データでのパフォーマンス", () => {
  const startTime = performance.now();

  const largeDataset = createUsers(10000);
  const result = processUsers(largeDataset);

  const endTime = performance.now();
  const executionTime = endTime - startTime;

  expect(executionTime).toBeLessThan(100); // 100ms以内
  expect(result).toHaveLength(10000);
});

test("メモリリークがない", () => {
  const wrapper = mount(HeavyComponent);
  const initialMemory = performance.memory?.usedJSHeapSize;

  // 大量のデータを処理
  wrapper.vm.processLargeData();

  // コンポーネントを破棄
  wrapper.unmount();

  // ガベージコレクションを強制実行
  if (global.gc) {
    global.gc();
  }

  const finalMemory = performance.memory?.usedJSHeapSize;

  // メモリ使用量が大幅に増加していないことを確認
  if (initialMemory && finalMemory) {
    const memoryIncrease = finalMemory - initialMemory;
    expect(memoryIncrease).toBeLessThan(1024 * 1024); // 1MB以内
  }
});
```

## CI/CD でのテスト運用

### テスト並列化

```typescript
// vitest.config.ts
export default defineConfig({
  test: {
    // CPUコア数に基づいて並列実行
    maxConcurrency: os.cpus().length,

    // テストファイルを並列実行
    pool: "threads",
    poolOptions: {
      threads: {
        maxThreads: os.cpus().length,
        minThreads: 1,
      },
    },
  },
});
```

### テストレポート

```yaml
# .github/workflows/test.yml
- name: Run tests with coverage
  run: npm run test -- --coverage --reporter=verbose --reporter=junit --reporter=json

- name: Upload coverage to Codecov
  uses: codecov/codecov-action@v3
  with:
    file: ./coverage/lcov.info
```

## テストの基本的な考え方

### 何をテストするか

**外部仕様（パブリック API）**

- コンポーネントの props → 表示内容
- ユーザーの操作 → イベント発火、状態変更
- API レスポンス → UI の変化

**テスト可能な設計**

```typescript
// ❌ テストしにくい設計
export default defineComponent({
  setup() {
    const handleClick = () => {
      // 複雑なロジックが直接書かれている
      if (
        user.value.role === "admin" &&
        user.value.permissions.includes("write")
      ) {
        showModal.value = true;
        analyticsTracker.track("admin_action");
        localStorage.setItem("lastAction", Date.now().toString());
      }
    };

    return { handleClick };
  },
});

// ✅ テストしやすい設計
export default defineComponent({
  setup() {
    const { canPerformAction } = usePermissions();
    const { track } = useAnalytics();
    const { saveLastAction } = useLocalStorage();

    const handleClick = () => {
      if (canPerformAction("write")) {
        showModal.value = true;
        track("admin_action");
        saveLastAction();
      }
    };

    return { handleClick };
  },
});
```

### 何をテストしないか

**内部実装の詳細**

- private メソッドの内部ロジック
- コンポーネントの内部状態（data, ref）
- 算出プロパティの計算過程

**フレームワークの機能**

- Vue.js の響応性システム
- ルーターの基本機能
- 外部ライブラリの動作

### テストの保守性

**良いテストの特徴**

1. **Independent（独立性）**: 他のテストの実行結果に依存しない
2. **Repeatable（再現性）**: 何度実行しても同じ結果
3. **Self-validating（自己検証）**: 成功/失敗が明確
4. **Timely（適時性）**: 機能実装と同時に書く
5. **Fast（高速）**: 素早くフィードバックを得られる

```typescript
// ❌ 悪い例: テスト間で状態を共有
describe("UserService", () => {
  let users: User[] = [];

  test("ユーザーを追加", () => {
    users.push(createUser());
    expect(users).toHaveLength(1);
  });

  test("ユーザーを削除", () => {
    // 前のテストの結果に依存している
    users.pop();
    expect(users).toHaveLength(0);
  });
});

// ✅ 良い例: 各テストが独立
describe("UserService", () => {
  test("ユーザーを追加", () => {
    const service = new UserService();
    service.addUser(createUser());
    expect(service.getUsers()).toHaveLength(1);
  });

  test("ユーザーを削除", () => {
    const service = new UserService();
    const user = createUser();
    service.addUser(user);
    service.removeUser(user.id);
    expect(service.getUsers()).toHaveLength(0);
  });
});
```

## まとめ

効果的なテストは以下の価値を提供します：

1. **品質保証**: バグの早期発見と回帰防止
2. **設計改善**: テストしやすい設計は良い設計
3. **開発速度向上**: 安心してリファクタリングできる
4. **ドキュメンテーション**: テストは生きた仕様書
5. **チーム協働**: 共通理解の基盤

テストは「書く」ことよりも「設計」することが重要です。何をどのようにテストするかを明確にし、保守可能で価値のあるテストスイートを構築していきましょう。
