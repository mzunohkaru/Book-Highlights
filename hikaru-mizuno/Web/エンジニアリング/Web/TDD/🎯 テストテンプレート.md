```typescript
import { mount } from '@vue/test-utils'
import YourComponent from '@/components/YourComponent.vue'

describe('YourComponent', () => {
  
  // 🟢 基本的な表示テスト
  describe('基本的な表示', () => {
    test('正常なデータで正しく表示される', () => {
      // Arrange
      const props = {
        // ここにテスト用のpropsを書く
      }
      
      // Act
      const wrapper = mount(YourComponent, { props })
      
      // Assert
      expect(wrapper.find('[data-testid="main-content"]').exists()).toBe(true)
      // 他のアサーションを追加
    })
  })

  // 🔴 エラー・境界値テスト
  describe('境界値・エラーケース', () => {
    test('空データでエラーにならない', () => {
      const wrapper = mount(YourComponent, {
        props: { data: [] }
      })
      
      expect(wrapper.find('[data-testid="empty-state"]').exists()).toBe(true)
    })

    test('nullデータでエラーにならない', () => {
      expect(() => {
        mount(YourComponent, {
          props: { data: null }
        })
      }).not.toThrow()
    })
  })

  // 🎭 インタラクション・動的テスト
  describe('ユーザーインタラクション', () => {
    test('ボタンクリックで期待した動作をする', async () => {
      // Arrange
      const wrapper = mount(YourComponent)
      
      // Act
      await wrapper.find('[data-testid="action-button"]').trigger('click')
      
      // Assert
      expect(wrapper.emitted('button-clicked')).toBeTruthy()
    })

    test('propsが変更された時、表示が更新される', async () => {
      // Arrange
      const wrapper = mount(YourComponent, {
        props: { status: 'loading' }
      })
      
      // Act
      await wrapper.setProps({ status: 'success' })
      
      // Assert
      expect(wrapper.find('[data-testid="success-message"]').exists()).toBe(true)
    })
  })

  // 🎪 特殊ケース・ビジネスロジック
  describe('ビジネスロジック', () => {
    // プロジェクト固有のロジックをここに書く
    test('特定の条件で特別な表示をする', () => {
      // あなたのアプリ固有のテストをここに
    })
  })
})

// 🎓 使い方ガイド
1. YourComponentを実際のコンポーネント名に変更
2. data-testidを実際のものに変更  
3. プロジェクトに合わせてテストケースを追加
4. 最初は1-2個のテストから始めて、徐々に増やす
```